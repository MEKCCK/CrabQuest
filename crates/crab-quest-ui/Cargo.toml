[package]
name = "crab-quest-ui"
version.workspace = true
edition.workspace = true

[dependencies]
crab-quest-core = { path = "../crab-quest-core" }
crab-quest-data = { path = "../crab-quest-data" }
macroquad = "0.4"
egui-macroquad = "0.17"
# 仅用于解码内嵌的本地图标 PNG；不包含网络加载功能。
image = { version = "0.24", default-features = false, features = ["png"] }
